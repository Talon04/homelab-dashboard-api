version: "3.8"

services:
  dashboard:
    image: ghcr.io/talon04/homelab-dashboard-api:latest
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app/backend
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
